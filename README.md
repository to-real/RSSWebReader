# RSS Web Reader

RSS 聚合 + AI 摘要 + 中文解读

## 开发

```bash
# Backend
cd backend
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt

# Frontend
cd frontend
npm install
npm run dev
```

## 部署

Railway 自动部署，推送即可。
